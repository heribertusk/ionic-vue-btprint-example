import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-btprint',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
